---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {tags.map((tag) => (
    <a 
      href={`/blog/tags/${tag.toLowerCase()}`}
      class="tag"
    >
      #{tag}
    </a>
  ))}
</div>
